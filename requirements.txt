# ── Verifiable DeFi Risk Guard — Streamlit Cloud requirements ─────────────────
#
# Rules applied:
#  • opengradient is listed first — heaviest dep tree, sets the baseline.
#  • web3 and eth-account are NOT pinned: opengradient==0.4.14 requires
#    eth-account>=0.13.4, pinning 0.11.0 causes ResolutionImpossible on Cloud.
#    Let pip resolve them as transitive dependencies.
#  • numpy capped <2.0.0 — numpy 2.x breaks SDK internals.
#  • No Windows-only packages (colorama, readline, pywin32 removed).

# ── OpenGradient SDK ──────────────────────────────────────────────────────────
opengradient==0.4.14

# ── LangChain integration ─────────────────────────────────────────────────────
langchain-opengradient==0.1.2
langchain-core==0.3.83

# ── Numerics — capped <2.0.0; numpy 2.x breaks SDK internals ─────────────────
numpy>=1.21.6,<2.0.0

# ── Local ONNX inference (Hybrid Mode fallback) ───────────────────────────────
onnxruntime==1.24.3

# ── HTTP / environment ────────────────────────────────────────────────────────
requests==2.32.5
python-dotenv==1.0.1

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit==1.54.0
plotly==6.5.2
